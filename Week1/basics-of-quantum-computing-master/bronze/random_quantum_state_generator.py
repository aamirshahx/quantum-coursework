# your function is here
from math import cos, sin, pi
from random import randrange
def random_quantum_state():
    angle = randrange(360)
    radians = 2*pi*myangle/360
    x = cos(radians)
    y = sin(radians)
    
    return [x, y]
